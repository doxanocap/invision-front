services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      - API_BASE_URL=${API_BASE_URL:-http://192.168.1.109:8000}
      - TZ=${TZ:-Asia/Almaty}
    command: npm run dev
    restart: unless-stopped
